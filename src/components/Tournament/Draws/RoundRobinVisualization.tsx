import React from "react";
import {Model} from "../Model";

type Props = {
    model:Model
}

const RoundRobinVisualization:(props: Props) => void = (props:Props) => {
    props.model

}
export default RoundRobinVisualization